# 🦄 Unichain Node - Установка в один клик

Автоматический скрипт для установки и управления Unichain Node - DeFi-native Ethereum L2 решение от команды Uniswap.

---

## 🎯 Возможности

- ✅ Автоматическая установка всех зависимостей (Docker, Docker Compose)
- ✅ Поддержка Mainnet и Sepolia Testnet
- ✅ Настройка публичных или собственных RPC эндпоинтов
- ✅ Простое управление нодой (старт/стоп/рестарт)
- ✅ Мониторинг статуса и просмотр логов
- ✅ Обновление и удаление ноды
- ✅ Интерактивное меню с подсказками

---

## 📋 Системные требования

| Компонент | Минимум | Рекомендуется |
|-----------|---------|---------------|
| **CPU** | 4 ядра | 8+ ядер |
| **RAM** | 8 GB | 16+ GB |
| **Диск** | 100 GB | 200+ GB SSD |
| **ОС** | Ubuntu 20.04+ | Ubuntu 22.04+ |
| **Сеть** | Стабильное интернет-соединение | |

**Важно:** Для Mainnet требуется больше ресурсов, так как нода будет синхронизироваться с основной сетью.

---

## 🚀 Быстрый старт

### Шаг 1: Скачайте скрипт

```bash
# Создайте файл
nano unichain_setup.sh

# Вставьте содержимое скрипта
# Сохраните: Ctrl+X, затем Y, затем Enter
```

### Шаг 2: Сделайте скрипт исполняемым

```bash
chmod +x unichain_setup.sh
```

### Шаг 3: Запустите установку

```bash
./unichain_setup.sh
```

---

## 📖 Подробная инструкция

### 1️⃣ Установка ноды

```bash
./unichain_setup.sh
# Выберите: 1 - Установить Unichain ноду
```

**Процесс установки:**

1. **Выбор сети**
   - Mainnet (основная сеть) - для продакшн использования
   - Sepolia (тестовая сеть) - для тестирования

2. **Проверка требований**
   - Скрипт проверит CPU, RAM и свободное место

3. **Установка зависимостей**
   - Обновление системы
   - Установка Docker
   - Установка Docker Compose
   - Клонирование репозитория

4. **Настройка RPC эндпоинтов**
   
   Вам понадобятся два L1 Ethereum RPC эндпоинта:
   - **Execution Layer RPC** (например: Infura, Alchemy, или публичный)
   - **Beacon Chain RPC** (консенсус слой)

   **Опции:**
   
   **a) Публичные RPC (бесплатно):**
   ```
   Sepolia:
   - ETH RPC: https://ethereum-sepolia-rpc.publicnode.com
   - Beacon: https://ethereum-sepolia-beacon-api.publicnode.com
   
   Mainnet:
   - ETH RPC: https://ethereum-rpc.publicnode.com
   - Beacon: https://ethereum-beacon-api.publicnode.com
   ```

   **b) Собственные RPC (рекомендуется):**
   
   Получите API ключи от провайдеров:
   - [Infura](https://infura.io) - бесплатный тариф доступен
   - [Alchemy](https://alchemy.com) - бесплатный тариф доступен
   - [QuickNode](https://quicknode.com)

   Пример URL:
   ```
   ETH RPC: https://sepolia.infura.io/v3/YOUR_API_KEY
   Beacon: https://sepolia.beacon.infura.io/v3/YOUR_API_KEY
   ```

5. **Редактирование docker-compose.yml**
   
   Скрипт откроет файл в редакторе nano. Вам нужно:
   
   **Для Sepolia (тестовая сеть):**
   ```yaml
   env_file:
     - .env
     - .env.sepolia      # Раскомментируйте (уберите #)
     # - .env.mainnet    # Закомментируйте (добавьте #)
   ```

   **Для Mainnet (основная сеть):**
   ```yaml
   env_file:
     - .env
     - .env.mainnet      # Раскомментируйте (уберите #)
     # - .env.sepolia    # Закомментируйте (добавьте #)
   ```

   **Важно:** Нужно отредактировать ДВЕ секции:
   - `execution-client:` → `env_file:`
   - `op-node:` → `env_file:`

   Сохраните: **Ctrl+X**, затем **Y**, затем **Enter**

### 2️⃣ Запуск ноды

```bash
./unichain_setup.sh
# Выберите: 2 - Запустить ноду
```

Нода начнёт синхронизироваться с сетью. Это может занять от нескольких часов до нескольких дней в зависимости от сети и скорости интернета.

### 3️⃣ Проверка статуса

```bash
./unichain_setup.sh
# Выберите: 4 - Проверить статус ноды
```

Вы увидите:
- Статус контейнеров (running/stopped)
- Текущий номер блока
- Доступность JSON-RPC

### 4️⃣ Просмотр логов

```bash
./unichain_setup.sh
# Выберите: 5 - Просмотреть логи
```

Выберите контейнер:
- **execution-client** - логи Geth (Ethereum клиент)
- **op-node** - логи OP Stack (Optimism)
- **Все логи** - оба контейнера

**Выход из логов:** Нажмите **Ctrl+C**

---

## 🔧 Управление нодой

### Остановка ноды

```bash
./unichain_setup.sh
# Выберите: 3 - Остановить ноду
```

### Обновление ноды

```bash
./unichain_setup.sh
# Выберите: 8 - Обновить ноду
```

Скрипт:
1. Остановит ноду
2. Получит последние обновления из GitHub
3. Обновит Docker образы
4. Запустит ноду

### Удаление ноды

```bash
./unichain_setup.sh
# Выберите: 9 - Удалить ноду
```

⚠️ **Внимание:** Это удалит все файлы ноды и данные блокчейна!

---

## 📊 Проверка работы ноды

### Через скрипт

```bash
./unichain_setup.sh
# Выберите: 4 - Проверить статус ноды
```

### Вручную через curl

**Получить номер текущего блока:**

```bash
curl -d '{"id":1,"jsonrpc":"2.0","method":"eth_blockNumber","params":[]}' \
  -H "Content-Type: application/json" http://localhost:8545
```

**Получить последний блок:**

```bash
curl -d '{"id":1,"jsonrpc":"2.0","method":"eth_getBlockByNumber","params":["latest",false]}' \
  -H "Content-Type: application/json" http://localhost:8545
```

**Проверить синхронизацию:**

```bash
curl -d '{"id":1,"jsonrpc":"2.0","method":"eth_syncing","params":[]}' \
  -H "Content-Type: application/json" http://localhost:8545
```

Ответ:
- `false` - нода полностью синхронизирована ✅
- Объект с данными - нода синхронизируется ⏳

---

## 🔑 Node Key (Приватный ключ)

Node Key - это уникальный идентификатор вашей ноды в P2P сети.

**Просмотр:**

```bash
./unichain_setup.sh
# Выберите: 7 - Показать Node Key
```

⚠️ **Важно:**
- Храните ключ в безопасности
- Не показывайте его публично
- Сделайте резервную копию

**Местоположение файла:**
```
~/unichain-node/geth-data/geth/nodekey
```

---

## 🌐 Эндпоинты и порты

После запуска ноды доступны следующие эндпоинты:

| Протокол | Порт | URL | Описание |
|----------|------|-----|----------|
| HTTP JSON-RPC | 8545 | http://localhost:8545 | Основной эндпоинт |
| WebSocket | 8546 | ws://localhost:8546 | WebSocket подключения |
| P2P | 30303 | - | Peer-to-peer сеть |

### Использование в приложениях

**Web3.js:**
```javascript
const Web3 = require('web3');
const web3 = new Web3('http://localhost:8545');
```

**Ethers.js:**
```javascript
const { ethers } = require('ethers');
const provider = new ethers.JsonRpcProvider('http://localhost:8545');
```

**MetaMask:**
```
Network Name: Unichain Local
RPC URL: http://localhost:8545
Chain ID: (зависит от сети)
Currency Symbol: ETH
```

---

## 📁 Структура файлов

```
~/unichain-node/
├── docker-compose.yml          # Конфигурация Docker
├── .env                        # Общие переменные окружения
├── .env.mainnet               # Настройки для Mainnet
├── .env.sepolia               # Настройки для Sepolia
├── geth-data/                 # Данные блокчейна
│   └── geth/
│       └── nodekey           # Приватный ключ ноды
└── op-geth-entrypoint.sh     # Скрипт запуска
```

---

## 🛠️ Решение проблем

### Нода не запускается

**1. Проверьте логи:**

```bash
cd ~/unichain-node
docker-compose logs execution-client
docker-compose logs op-node
```

**2. Проверьте конфигурацию:**

```bash
cd ~/unichain-node
docker-compose config
```

**3. Проверьте, что порты свободны:**

```bash
sudo netstat -tulpn | grep -E "8545|8546|30303"
```

### Ошибка "Cannot connect to L1"

**Проблема:** Неверные или недоступные L1 RPC эндпоинты.

**Решение:**

1. Проверьте RPC в файле `.env.sepolia` или `.env.mainnet`:
   ```bash
   cd ~/unichain-node
   cat .env.sepolia
   ```

2. Отредактируйте файл:
   ```bash
   nano .env.sepolia
   ```

3. Обновите значения:
   ```
   OP_NODE_L1_ETH_RPC=https://ваш-новый-rpc
   OP_NODE_L1_BEACON=https://ваш-новый-beacon
   ```

4. Перезапустите ноду:
   ```bash
   docker-compose down
   docker-compose up -d
   ```

### Нода очень медленно синхронизируется

**Возможные причины:**

1. **Медленный интернет** - требуется стабильное подключение
2. **Слабое железо** - проверьте требования
3. **Медленные L1 RPC** - используйте быстрые платные RPC

**Решение:**

- Используйте платные RPC от Infura/Alchemy/QuickNode
- Увеличьте ресурсы сервера
- Дождитесь полной синхронизации (может занять дни)

### Ошибка "Out of memory"

**Решение:**

1. Увеличьте RAM сервера (минимум 8GB, рекомендуется 16GB)
2. Добавьте swap:
   ```bash
   sudo fallocate -l 8G /swapfile
   sudo chmod 600 /swapfile
   sudo mkswap /swapfile
   sudo swapon /swapfile
   ```

### Docker не запускается

**Решение:**

```bash
# Перезапустите Docker
sudo systemctl restart docker

# Проверьте статус
sudo systemctl status docker

# Примените права
sudo usermod -aG docker $USER
newgrp docker
```

---

## 📊 Мониторинг производительности

### Использование ресурсов

**Через Docker:**
```bash
docker stats
```

**Через htop:**
```bash
sudo apt install htop
htop
```

### Размер данных

```bash
du -sh ~/unichain-node/geth-data
```

### Логи Docker

```bash
cd ~/unichain-node
docker-compose logs --tail=100 execution-client
docker-compose logs --tail=100 op-node
```

---

## 🔐 Безопасность

### Рекомендации:

1. **Firewall**
   ```bash
   # Разрешите только необходимые порты
   sudo ufw allow 22/tcp   # SSH
   sudo ufw allow 30303    # P2P (опционально)
   sudo ufw enable
   
   # НЕ открывайте порты 8545, 8546 публично!
   ```

2. **SSH ключи**
   - Используйте SSH ключи вместо паролей
   - Отключите root login

3. **Обновления**
   ```bash
   # Регулярно обновляйте систему
   sudo apt update && sudo apt upgrade -y
   ```

4. **Мониторинг**
   - Следите за логами
   - Настройте алерты при падении ноды

5. **Резервные копии**
   ```bash
   # Сохраните nodekey
   cp ~/unichain-node/geth-data/geth/nodekey ~/nodekey.backup
   ```

---

## 🔄 Обслуживание

### Регулярные задачи

**Еженедельно:**
- Проверяйте статус ноды
- Просматривайте логи на ошибки
- Проверяйте использование диска

**Ежемесячно:**
- Обновляйте ноду до последней версии
- Обновляйте систему
- Проверяйте резервные копии

**По необходимости:**
- Увеличивайте дисковое пространство
- Оптимизируйте производительность

### Очистка старых логов

```bash
# Удаление старых Docker логов
docker system prune -a --volumes

# Ограничение размера логов (добавьте в docker-compose.yml)
logging:
  driver: "json-file"
  options:
    max-size: "100m"
    max-file: "3"
```

---

## 📚 Полезные ссылки

- 🌐 [Официальный сайт Unichain](https://unichain.org)
- 📖 [Документация Unichain](https://docs.unichain.org)
- 💬 [Discord сообщество](https://discord.gg/uniswap)
- 🐙 [GitHub репозиторий](https://github.com/Uniswap/unichain-node)
- 📊 [Block Explorer (Mainnet)](https://unichain.org/explorer)
- 📊 [Block Explorer (Sepolia)](https://sepolia.unichain.org/explorer)

### RPC провайдеры:

- [Infura](https://infura.io)
- [Alchemy](https://alchemy.com)
- [QuickNode](https://quicknode.com)
- [Publicnode (бесплатные)](https://publicnode.com)

### Инструменты мониторинга:

- [Grafana + Prometheus](https://grafana.com)
- [Netdata](https://www.netdata.cloud)
- [Uptime Kuma](https://github.com/louislam/uptime-kuma)

---

## ❓ FAQ

### Сколько времени занимает синхронизация?

- **Sepolia (testnet):** 2-6 часов
- **Mainnet:** 12-48 часов

Зависит от скорости интернета и L1 RPC.

### Сколько места на диске нужно?

- **Sepolia:** ~50-100 GB
- **Mainnet:** ~200-500 GB

Размер постоянно растёт.

### Можно ли использовать VPS?

Да! Рекомендуемые провайдеры:
- Contabo (дёшево)
- Hetzner (баланс цена/качество)
- DigitalOcean (надёжно)
- AWS/GCP (дорого, но стабильно)

### Нужен ли мне статический IP?

Нет, для базовой работы ноды это не требуется.

### Могу ли я использовать ноду для своего dApp?

Да! Используйте эндпоинт `http://localhost:8545` в вашем приложении.

### Как сменить сеть (Sepolia → Mainnet)?

1. Остановите ноду
2. Отредактируйте `docker-compose.yml`
3. Раскомментируйте нужную сеть
4. Обновите RPC в `.env.mainnet`
5. Запустите ноду

---

## 🤝 Поддержка

### Если у вас проблемы:

1. **Проверьте логи:** `./unichain_setup.sh` → опция 5
2. **Изучите FAQ** в этом README
3. **Поищите в Issues:** [GitHub Issues](https://github.com/Uniswap/unichain-node/issues)
4. **Спросите в Discord:** [Uniswap Discord](https://discord.gg/uniswap)

### Баг репорты

Если нашли баг, создайте issue в [официальном репозитории](https://github.com/Uniswap/unichain-node/issues).

---

## 📝 Changelog

**v1.0** - Первая версия
- Установка Unichain Node
- Поддержка Mainnet и Sepolia
- Интерактивное меню
- Автоматическая настройка RPC
- Управление нодой (старт/стоп/обновление)
- Мониторинг и логи

---

## ⚖️ Лицензия

Скрипт распространяется "как есть" без каких-либо гарантий. Используйте на свой риск.

Unichain Node разработан командой Uniswap и распространяется под их лицензией.

---

**Удачного запуска вашей Unichain ноды! 🦄🚀**
