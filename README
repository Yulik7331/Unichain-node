# 🦄 Unichain Node Setup

Автоматический скрипт установки и управления Unichain Node - DeFi-native Ethereum Layer 2 решение от Uniswap.

---

## Возможности

- Автоматическая установка Docker и Docker Compose
- Поддержка Mainnet и Sepolia Testnet
- Настройка публичных или собственных RPC эндпоинтов
- Простое управление нодой через интерактивное меню
- Мониторинг статуса и просмотр логов
- Обновление и удаление ноды

---

## Системные требования

| Компонент | Минимум | Рекомендуется |
|-----------|---------|---------------|
| CPU | 4 ядра | 8+ ядер |
| RAM | 8 GB | 16+ GB |
| Диск | 100 GB | 200+ GB SSD |
| ОС | Ubuntu 20.04+ | Ubuntu 22.04+ |

> **Примечание:** Для Mainnet требуется больше ресурсов из-за объема данных блокчейна.

---

## Быстрый старт

### 1. Скачайте скрипт

```bash
wget https://your-url/unichain_setup.sh
# или создайте вручную:
nano unichain_setup.sh
```

### 2. Сделайте исполняемым

```bash
chmod +x unichain_setup.sh
```

### 3. Запустите

```bash
./unichain_setup.sh
```

---

## Установка ноды

### Шаг 1: Выберите сеть

При запуске скрипта выберите опцию **1 - Установить Unichain ноду**, затем:

- **Mainnet** - для продакшена
- **Sepolia** - для тестирования

### Шаг 2: Настройка RPC эндпоинтов

Вам понадобятся два Ethereum L1 RPC:

**Публичные RPC (бесплатно):**

Для Sepolia:
```
ETH RPC: https://ethereum-sepolia-rpc.publicnode.com
Beacon: https://ethereum-sepolia-beacon-api.publicnode.com
```

Для Mainnet:
```
ETH RPC: https://ethereum-rpc.publicnode.com
Beacon: https://ethereum-beacon-api.publicnode.com
```

**Собственные RPC (рекомендуется):**

Получите ключи от провайдеров:
- [Infura](https://infura.io) - бесплатный план
- [Alchemy](https://alchemy.com) - бесплатный план
- [QuickNode](https://quicknode.com)

Формат URL:
```
https://sepolia.infura.io/v3/YOUR_API_KEY
https://sepolia.beacon.infura.io/v3/YOUR_API_KEY
```

### Шаг 3: Редактирование docker-compose.yml

Скрипт откроет nano редактор. Найдите секции `execution-client` и `op-node`, затем в каждой:

Для Sepolia:
```yaml
env_file:
  - .env
  - .env.sepolia      # уберите #
  # - .env.mainnet    # добавьте #
```

Для Mainnet:
```yaml
env_file:
  - .env
  - .env.mainnet      # уберите #
  # - .env.sepolia    # добавьте #
```

Сохраните: `Ctrl+X` → `Y` → `Enter`

### Шаг 4: Запустите ноду

Выберите опцию **2 - Запустить ноду**

Синхронизация займет:
- Sepolia: 2-6 часов
- Mainnet: 12-48 часов

---

## Управление нодой

### Запуск
```bash
./unichain_setup.sh
# Опция 2
```

### Остановка
```bash
./unichain_setup.sh
# Опция 3
```

### Проверка статуса
```bash
./unichain_setup.sh
# Опция 4
```

### Просмотр логов
```bash
./unichain_setup.sh
# Опция 5
```

### Обновление
```bash
./unichain_setup.sh
# Опция 8
```

---

## Проверка работы

### Через скрипт

Используйте опцию **4 - Проверить статус ноды**

### Через curl

Проверить номер блока:
```bash
curl -d '{"id":1,"jsonrpc":"2.0","method":"eth_blockNumber","params":[]}' \
  -H "Content-Type: application/json" http://localhost:8545
```

Проверить синхронизацию:
```bash
curl -d '{"id":1,"jsonrpc":"2.0","method":"eth_syncing","params":[]}' \
  -H "Content-Type: application/json" http://localhost:8545
```

Ответ `false` означает полную синхронизацию ✅

---

## Эндпоинты

После запуска доступны:

- **JSON-RPC:** `http://localhost:8545`
- **WebSocket:** `ws://localhost:8546`
- **P2P:** порт `30303`

### Использование в коде

Web3.js:
```javascript
const Web3 = require('web3');
const web3 = new Web3('http://localhost:8545');
```

Ethers.js:
```javascript
const { ethers } = require('ethers');
const provider = new ethers.JsonRpcProvider('http://localhost:8545');
```

---

## Структура файлов

```
~/unichain-node/
├── docker-compose.yml      # Docker конфигурация
├── .env                    # Общие переменные
├── .env.mainnet           # Настройки Mainnet
├── .env.sepolia           # Настройки Sepolia
├── geth-data/             # Данные блокчейна
│   └── geth/
│       └── nodekey       # Приватный ключ ноды
└── op-geth-entrypoint.sh # Скрипт запуска
```

---

## Решение проблем

### Нода не запускается

Проверьте логи:
```bash
cd ~/unichain-node
docker-compose logs execution-client
docker-compose logs op-node
```

Проверьте порты:
```bash
sudo netstat -tulpn | grep -E "8545|8546|30303"
```

### Ошибка подключения к L1

Проверьте RPC эндпоинты:
```bash
cd ~/unichain-node
nano .env.sepolia  # или .env.mainnet
```

Убедитесь что:
- RPC URL корректные
- API ключи валидные
- Эндпоинты доступны

### Медленная синхронизация

Возможные причины:
- Медленный интернет
- Слабое железо
- Медленные L1 RPC

Решение:
- Используйте платные RPC (Infura/Alchemy)
- Увеличьте ресурсы сервера
- Дождитесь завершения синхронизации

### Ошибка памяти

Добавьте swap:
```bash
sudo fallocate -l 8G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
```

---

## Безопасность

### Firewall

```bash
sudo ufw allow 22/tcp   # SSH
sudo ufw allow 30303    # P2P
sudo ufw enable
```

> **Важно:** НЕ открывайте порты 8545, 8546 публично!

### Резервная копия

Сохраните приватный ключ:
```bash
cp ~/unichain-node/geth-data/geth/nodekey ~/nodekey.backup
```

### Обновления

Регулярно обновляйте систему:
```bash
sudo apt update && sudo apt upgrade -y
```

---

## Мониторинг

### Ресурсы

Через Docker:
```bash
docker stats
```

Через htop:
```bash
sudo apt install htop
htop
```

### Размер данных

```bash
du -sh ~/unichain-node/geth-data
```

### Логи

```bash
cd ~/unichain-node
docker-compose logs --tail=100 execution-client
```

---

## FAQ

**Сколько занимает синхронизация?**
- Sepolia: 2-6 часов
- Mainnet: 12-48 часов

**Сколько места нужно?**
- Sepolia: ~50-100 GB
- Mainnet: ~200-500 GB (растет со временем)

**Можно использовать VPS?**
Да. Рекомендуемые провайдеры:
- Contabo (бюджетный)
- Hetzner (оптимальный)
- DigitalOcean (надежный)

**Нужен ли статический IP?**
Нет, для базовой работы не требуется.

**Можно использовать для dApp?**
Да, подключайтесь к `http://localhost:8545`

---

## Полезные ссылки

- [Официальный сайт Unichain](https://unichain.org)
- [Документация](https://docs.unichain.org)
- [GitHub репозиторий](https://github.com/Uniswap/unichain-node)
- [Discord сообщество](https://discord.gg/uniswap)

### RPC провайдеры

- [Infura](https://infura.io)
- [Alchemy](https://alchemy.com)
- [QuickNode](https://quicknode.com)
- [Publicnode](https://publicnode.com) (бесплатный)

---

## Поддержка

Если возникли проблемы:

1. Проверьте логи через опцию 5
2. Изучите раздел FAQ
3. Посмотрите [GitHub Issues](https://github.com/Uniswap/unichain-node/issues)
4. Спросите в [Discord](https://discord.gg/uniswap)

---

## Лицензия

Скрипт распространяется "как есть" без гарантий. Используйте на свой риск.

Unichain Node разработан командой Uniswap.

---

**Удачного запуска! 🦄🚀**
